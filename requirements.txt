fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
# Optional: install for LLM-generated Scribe outputs.
# Also set ANTHROPIC_API_KEY in your environment.
# Without it, Scribe falls back to deterministic template outputs.
anthropic>=0.28.0
